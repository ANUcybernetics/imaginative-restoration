[tools]
erlang = "28.1.1"
elixir = "1.18.4-otp-28"
sqlite = "latest"
fnox = "latest"

[env]
AWS_ENDPOINT_URL_S3 = "https://fly.storage.tigris.dev"
AWS_REGION = "auto"
# AUTH_USERNAME and AUTH_PASSWORD live in mise.local.toml (gitignored).
# Real prod creds are set as Fly secrets.
